# 로봇 실행 명령어

로봇: 100.103.150.5 / SSH 포트: 2020. 기존 .bashrc의 ROS·Tailscale 설정을 사용합니다.
방향 수정 적용을 위해 이전 매핑/제어/Nav2 실행 터미널에서 Ctrl+C로 종료한 뒤 시작하세요.
제어·센서는 통합 시작 명령이 함께 실행하므로 별도로 켜지 않습니다.

## 1. 로봇에서 매핑 시작

노트북 터미널에서 접속:

```bash
ssh -p 2020 dori@100.103.150.5
```

접속한 로봇 터미널에서:

```bash
cd /home/dori/robot_ws
./scripts/start_mapping.sh
```

sudo 비밀번호가 나오면 입력합니다. 제어·조이스틱·odometry·라이다·Cartographer가 함께 켜집니다.
처음에는 짧게 전진/좌회전 입력을 주어 실제 방향을 확인합니다. 현재 방향 수정은 코드·설정 검증까지 완료했으며 실물 회전은 아직 확인하지 않았습니다.

## 2. 노트북에서 RViz 실행

노트북 그래픽 데스크톱의 새 터미널에서 실행합니다. 로봇 SSH 터미널에서 RViz를 실행하지 않습니다.

```bash
mkdir -p ~/robot_remote
scp -P 2020 dori@100.103.150.5:/home/dori/robot_ws/src/robot_navigation/config/remote_monitor.rviz ~/robot_remote/
rviz2 -d ~/robot_remote/remote_monitor.rviz
```

조이스틱으로 움직이며 매핑합니다. 표시되는 지도 /map, 라이다 /scan, Fixed Frame=map.

## 3. 로봇에서 지도 저장

로봇을 정지한 뒤 다른 터미널에서 접속합니다.

```bash
ssh -p 2020 dori@100.103.150.5
```

```bash
cd /home/dori/robot_ws
./scripts/save_map.sh /home/dori/robot_ws/maps/site
```

site.yaml과 지도 이미지가 저장됩니다. 같은 이름의 기존 지도를 보존하려면 site 대신 다른 이름을 사용하세요.
저장이 성공하면 매핑 실행 터미널에서 Ctrl+C로 종료합니다.

## 4. 노트북에서 목적지 지정

SSH가 아닌 노트북 일반 터미널에서:

```bash
scp -P 2020 'dori@100.103.150.5:/home/dori/robot_ws/maps/site.*' ~/robot_remote/
scp -P 2020 dori@100.103.150.5:/home/dori/robot_ws/src/robot_navigation/scripts/named_points_editor.py ~/robot_remote/
python3 ~/robot_remote/named_points_editor.py --map_yaml ~/robot_remote/site.yaml --points_csv ~/robot_remote/site.points.csv
```

현재 지도 자유 공간에 이름 있는 목적지를 최소 1개 등록하고 저장합니다.
이전 site.points.csv가 있다면 새 지도에 맞는지 확인하고 이전 좌표를 그대로 사용하지 마세요.
편집기 의존성이 없다는 오류가 있을 때 노트북에서:

```bash
sudo apt install python3-matplotlib python3-pil python3-yaml
```

CSV 전송:

```bash
scp -P 2020 ~/robot_remote/site.points.csv dori@100.103.150.5:/home/dori/robot_ws/maps/
```

## 5. 로봇에서 Nav2 시작

로봇 SSH 터미널에서:

```bash
cd /home/dori/robot_ws
./scripts/start_navigation.sh --map /home/dori/robot_ws/maps/site.yaml --points /home/dori/robot_ws/maps/site.points.csv
```

제어와 센서도 함께 시작합니다. 매핑과 동시에 실행하지 않습니다.
초기 위치는 나중에 노트북 RViz의 2D Pose Estimate로 지정할 수 있습니다.
지도에서 실제 로봇 위치를 클릭하고 바라보는 방향으로 드래그하면 /initialpose가 AMCL로 전달됩니다.
초기 위치가 없으면 일부 Nav2 활성화가 대기하며 주행 준비가 완료되지 않습니다.

## 6. 준비 상태 확인

로봇의 다른 SSH 터미널에서:

```bash
cd /home/dori/robot_ws
./scripts/check_navigation.sh
```

"ok": true 및 RViz의 지도·라이다 정합을 확인합니다.

## 7. 노트북에서 목적지 발행 및 취소

노트북 일반 터미널에서 '목적지명'을 실제 CSV에 등록한 이름으로 변경합니다.

```bash
ros2 topic pub --once /NAVIGATION_TEXT std_msgs/msg/String "{data: '목적지명'}"
ros2 topic pub --once /NAVIGATION_MODE std_msgs/msg/Int32 '{data: 1}'
```

정확한 등록 이름은 API 없이 동작합니다. 자연어는 로봇 Nav2 시작 터미널의 OPENAI_API_KEY가 필요합니다.
RViz 직접 주행 목표 버튼은 현재 구동 연결 대상이 아닙니다. 초기 위치 지정 버튼은 지원합니다.

목표 취소·AUTO 정지:

```bash
ros2 topic pub --once /NAVIGATION_MODE std_msgs/msg/Int32 '{data: 0}'
```

로봇 터미널에서는 다음 단축 명령도 사용할 수 있습니다.

```bash
cd /home/dori/robot_ws
./scripts/navigate.sh '목적지명'
./scripts/stop_navigation.sh
```

전체 종료: start_mapping.sh 또는 start_navigation.sh 실행 터미널에서 Ctrl+C.

## 아직 실측할 설정

라이다 장착 위치, 바퀴 반지름·간격, 로봇 외곽은 실제 값 확인이 필요합니다.
방향 수정 전에 잘못된 odometry로 만든 지도는 새로 매핑하세요.
기존 코드·빌드 기준값이 실제 기구 보정을 대신하지 않습니다.

## 중복 odometry 오류 수정

`Existing stack detected: wheel_odometry_node`가 발생했던 원인은 이전 실행에서 부모가 종료된 뒤 odometry 자식만 남았기 때문입니다. 2026-09-08 해당 고아 프로세스를 정리하고, `scripts/robot_stack.py`가 부모 종료 후에도 자신이 시작한 프로세스 그룹의 남은 자식을 종료하도록 보완했습니다. 실행 스크립트 수정이므로 재빌드는 필요 없습니다.
다른 정상 실행이 있는 경우의 중복 차단은 유지됩니다. 기존 실행 터미널에서 Ctrl+C로 종료한 뒤 다시 시작하세요.

## robot_description 파싱 오류 수정

방향 수정 후 URDF 주석을 YAML로 잘못 해석해 제어 launch가 시작되지 않는 문제가 있었습니다. `system_real.launch.py` 및 같은 URDF를 사용하는 두 실험 launch에서 `ParameterValue(..., value_type=str)`를 적용했습니다. robot_bringup 재빌드와 설치된 세 launch의 실제 파라미터 평가 검증을 완료했습니다. 라이다만 수신되고 JointState/odom이 없었던 이번 세션의 직접 원인은 이 launch 실패였습니다.

## 최신 좌우 회전 수정

좌우 바퀴 배정을 CAN 주소와 방향 부호의 쌍으로 교환했습니다: left=CAN 2/+1, right=CAN 1/-1. 직전 설정의 전후진은 유지하고 회전 성분만 반전합니다. 실행을 Ctrl+C로 종료한 뒤 start_mapping.sh를 다시 실행해야 적용됩니다. 라이다 설정은 그대로입니다.

## 슬립이 클 때의 매핑 (2026-09-09)

기존 매핑을 Ctrl+C로 종료한 뒤 라이다 중심 매핑을 선택할 수 있습니다.

```bash
cd /home/dori/robot_ws
./scripts/start_mapping.sh --mapping-odometry lidar
```

바퀴 odom은 제어/진단용으로 유지하되 Cartographer 입력에서는 제외합니다. IMU는 아직 장착 축을 확인하지 못해 활성화하지 않았습니다. 정지 측정 결과와 IMU 결합 절차: [IMU_MAPPING.md](docs/IMU_MAPPING.md).

## 시작 직후 CAN/odom 준비 대기 (2026-09-09)

제어 시작 후 12초 단발 검사 대신 최대 60초 동안 센서/컨트롤러 준비를 재검사합니다. 양쪽 구동 컨트롤러와 JointState broadcaster가 active이고, 양 바퀴 CAN 피드백 및 odom/TF 검사를 통과한 뒤에만 매핑이 시작됩니다. 준비 중에는 Waiting for base readiness 메시지가 표시됩니다. 60초 후에도 실패하면 log/session_*/base_check_*.log 및 control.log를 확인하세요. CAN 신선도 기준을 완화하거나 우회하지 않습니다.

실기 재검증 결과: 새 대기 절차로 양쪽 구동 컨트롤러 active, 양 모터 CAN 응답, odom/TF 일치 검사 통과 후 라이다 중심 Cartographer가 시작됐습니다. /map 메시지 2개 수신(186×67 셀)을 확인하고 확인용 세션을 종료했습니다. 빈 imu_calibration_file 인자를 CLI로 전달하지 않도록 함께 수정했습니다.

## Odometry 배율 비교 옵션

기존 매핑을 Ctrl+C로 종료하고, 바퀴 odometry를 SLAM에 넣는 모드에서 배율을 지정합니다.

```bash
cd /home/dori/robot_ws
./scripts/start_mapping.sh --mapping-odometry wheel --odom-scale 0.1
```

원래 값 또는 중간 값으로 비교:

```bash
./scripts/start_mapping.sh --mapping-odometry wheel --odom-scale 1.0
./scripts/start_mapping.sh --mapping-odometry wheel --odom-scale 0.2
./scripts/start_mapping.sh --mapping-odometry wheel --odom-scale 0.5
```

위 명령은 한 번에 하나만 실행합니다. 바꿀 때마다 이전 실행을 Ctrl+C로 종료합니다. 기본값은 1.0이며 0 이하/NaN/Inf는 거부합니다.

배율은 encoder 차분으로 계산하는 이동량과 회전량 모두에 적용합니다. 0.1이면 같은 측정각에 대한 ds/dyaw 및 선속도/각속도가 각각 1/10이 됩니다. pose는 보정된 yaw로 다시 적분하므로 곡선 주행의 최종 x/y를 단순히 0.1배 한 결과와는 다를 수 있습니다.

/odom과 odom→base_link TF에 동일하게 적용합니다. 실제 바퀴 치수, CAN 명령, 조이스틱/Nav2 명령 스케일, 원본 /joint_states는 변경하지 않습니다. 이 옵션은 센서 해석의 실험적 보정값이며 실제 모터를 10배 느리게 돌리는 옵션이 아닙니다.

배율은 시작 시 고정됩니다. 실행 도중 ros2 param set으로 바꾸지 말고 재시작하세요. 활성값 확인은 로봇 터미널에서:

```bash
source /home/dori/robot_ws/scripts/env.sh
ros2 param get /wheel_odometry_node odom_scale
```

각 세션의 log/session_*/session_options.json에 실행 배율이 저장됩니다. 지도 파일도 site_scale01, site_scale02 등 서로 다른 이름으로 저장하면 비교하기 편합니다. 라이다 중심 모드(--mapping-odometry lidar)에서는 Cartographer가 /odom을 측정 입력으로 사용하지 않으므로, 이번 배율 비교는 wheel 모드로 수행하세요.

좋은 값은 같은 실제 이동거리·회전각에서 odometry가 맞고 벽 정합이 안정적인 값입니다. 지도 모양만으로 물리 스케일 교정 완료를 판단하지 않습니다. 공분산은 기존 보수적 설정을 유지하며 배율을 줄였다는 이유로 신뢰도를 높이지 않습니다.

## 1m 직진 odometry 실측 도구

기존 제어/매핑을 해당 터미널에서 Ctrl+C로 종료한 뒤 로봇 SSH 터미널에서 실행합니다.

```bash
cd /home/dori/robot_ws
./scripts/measure_1m.sh --odom-scale 1.0
```

제어·조이스틱·라이다·바퀴 odometry·base_link→laser TF가 함께 켜지고 CAN 준비도 수행합니다. 시작 검사가 통과하면:

1. 바닥의 출발선에서 로봇을 정지시키고 Enter.
2. 조이스틱으로 실제 바닥 기준 1m를 직진. 줄자/바닥 표시로 측정합니다.
3. 로봇을 완전히 정지한 뒤 Enter.
4. odometry 시작/끝 직선거리, 시작 방향 기준 전진·옆이동, 누적 경로, 오차(%)와 거리 기준 배율 후보가 표시됩니다.
5. 다음 출발점에서 Enter로 재측정하거나 q+Enter/Ctrl+C로 전체 세션을 종료합니다.

이 프로그램 자체는 cmd_vel을 발행하지 않습니다. Enter는 주행/정지 명령이 아니므로 반드시 조이스틱을 놓아 정지한 뒤 누릅니다. 기존 지도를 만들던 세션과 함께 실행하면 중복 차단됩니다. 이 측정 모드는 Cartographer 대신 /odom 자체를 기준으로 측정합니다. 노트북에서 라이다를 보려면 RViz Fixed Frame/Target Frame을 odom으로 지정하세요.

다른 조건:

```bash
# 현재 실험 중인 0.1 배율을 검증
./scripts/measure_1m.sh --odom-scale 0.1
# 실제 0.5m를 주행해 측정
./scripts/measure_1m.sh --distance 0.5 --odom-scale 1.0
```

결과는 log/session_*/odometry_distance_01.json 등에 저장됩니다. 배율 후보 = 현재 배율 × 실제 거리 ÷ odometry 직선거리. 자동 적용하지 않습니다. 후진·큰 회전·옆이동·우회 경로·미미한 이동은 배율 추천을 보류합니다. 수신 중단/역순 시각/odom 재시작은 측정을 무효 처리합니다. 직선 거리 교정이 회전 배율 검증을 대신하지 않습니다.

검증: 거리 계산 3개 단위 테스트(좌표 원점/초기 방향 독립, 배율 보정, 부적절한 측정 배제) 및 Python/bash 구문·CLI 검사 통과. 실제 1m 주행 결과는 사용자의 측정으로 확인합니다.

## 보정 IMU + 라이다 매핑 (2026-09-09)

기존 매핑 실행을 Ctrl+C로 종료한 뒤 로봇 터미널에서:

```bash
cd /home/dori/robot_ws
./scripts/start_mapping_imu.sh
```

제어·조이스틱·바퀴 odom·라이다를 함께 시작한다. SLAM은 `/scan`과 `/imu/mapping`을 사용하며, 과대 회전이 관측된 바퀴 odom은 SLAM 측정 입력에서 제외한다. 바퀴 `odom→base_link` TF는 유지하고 Cartographer가 `map→odom`을 보정한다. IMU만으로 위치를 적분하는 구성은 아니다.

보정 파일: `src/robot_mapping/config/imu_calibrated.yaml`.

- 원본 `/imu/data_raw`는 기존 몸체 제어용으로 유지.
- SLAM 전용 `/imu/mapping`: X=원본 Y, Y=원본 X, Z=-원본 Z, 자이로 정지 편향 제거. 원본 타임스탬프 유지.
- `imu_mapping`은 IMU 위치에서 몸체 축과 정렬된 좌표계. Cartographer tracking_frame도 `imu_mapping`으로 설정.
- IMU 위치는 사용자 추정값으로 몸체 중심에서 x=0, y=+0.10m, z=+0.10m. 정밀 실측값은 아니다.
- 재시작 후 편향이 달라질 수 있으므로 정지 상태 점검 필요. 지도 저장은 기존 `./scripts/save_map.sh /home/dori/robot_ws/maps/site` 사용.

라이브 계약 검사:

```bash
source /home/dori/robot_ws/scripts/env.sh
python3 tools/verify_mapping_imu.py --output docs/imu_mapping_live_check.json
```
