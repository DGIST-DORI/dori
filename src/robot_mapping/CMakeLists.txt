cmake_minimum_required(VERSION 3.8)
project(robot_mapping)
find_package(ament_cmake REQUIRED)
install(DIRECTORY launch config scripts maps DESTINATION share/${PROJECT_NAME} USE_SOURCE_PERMISSIONS)
install(PROGRAMS scripts/mapping_imu.py DESTINATION lib/${PROJECT_NAME})
install(FILES scripts/imu_calibration.py scripts/imu_rotation.py DESTINATION lib/${PROJECT_NAME})
ament_package()
